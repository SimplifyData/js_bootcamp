const is_raining = true;

// used where we need a constant number used repeatedly


const person = {
    age: 27
}

person.age = 28

// person = {} is not possible since person is a constant now

console.log(person);

// this makes code more logical and readable