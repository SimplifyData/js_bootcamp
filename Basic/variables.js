/* let pet_name = 'Hal';
let pet_name = 'Spot';

console.log(pet_name); it will throw an error */

// only use let once to define pet name twice

let pet_name = 'Spot';
pet_name = 'Tom';

console.log(pet_name);